# dsh-file bundle patch: registers the file manager plugin.
#
# A single loader row: `name: dsh-file` resolves the package main
# (dist/index.js), whose default export is the FileManagerGateway
# (TypertRemoteService) class — the host half. The browser half is discovered
# automatically: dsh-client-modules scans loader entries whose package
# declares `dsh.client` + `exports["./client"]`, and this package does, so
# dist/client.js is served at /plugins/dsh-file/client.js and seeded into
# window.__DSH_BOOT__.
#
# `root` pins the workspace the gateway serves (defaults to process.cwd(),
# i.e. the directory `dsh web` was launched from). Paths outside this root are
# rejected by the host.
- insert:
    - id: dsh-file
      name: 'dsh-file'
      config:
        root: !!js process.cwd()
